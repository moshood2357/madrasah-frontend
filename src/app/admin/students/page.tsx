import StudentsList from "@/components/admin-panel/students/StudentsList";

export const metadata = { title: "Students | Admin" };

export default function AdminStudentsPage() {
  return <StudentsList />;
}
