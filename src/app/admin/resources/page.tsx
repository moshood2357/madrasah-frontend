import ResourcesList from "@/components/admin-panel/resources/ResourcesList";

export const metadata = { title: "Resources | Admin" };

export default function AdminResourcesPage() {
  return <ResourcesList />;
}
